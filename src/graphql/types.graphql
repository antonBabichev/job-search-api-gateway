type Job {
    title: String!,
    url: String!,
    description: String
}

type Account {
    name: String!,
    email: String!
}

type Portal {
    name: String!,
    url: String!,
    accounts: [Account]
    jobs: [Job]
}

type Query {
  _version: String
}